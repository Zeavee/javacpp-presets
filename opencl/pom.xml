<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.oracle.svm.shadowed.org.bytedeco</groupId>
    <artifactId>javacpp-presets</artifactId>
    <version>1.5.8-SNAPSHOT</version>
  </parent>

  <groupId>com.oracle.svm.shadowed.org.bytedeco</groupId>
  <artifactId>opencl</artifactId>
  <version>3.0-${project.parent.version}</version>
  <name>JavaCPP Presets for OpenCL</name>

  <dependencies>
    <dependency>
      <groupId>com.oracle.svm.shadowed.org.bytedeco</groupId>
      <artifactId>javacpp</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>com.oracle.svm.shadowed.org.bytedeco</groupId>
        <artifactId>javacpp</artifactId>
      </plugin>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <id>javacpp-${javacpp.platform}</id>
            <phase>package</phase>
            <configuration>
              <excludes>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/*.exp</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/*.lib</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/*.obj</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/bin/*</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/lib/*.dll</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/lib/*.dylib</exclude>
                <exclude>com/oracle/svm/shadowed/org/bytedeco/opencl/${javacpp.platform}/lib/*.so*</exclude>
              </excludes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.moditect</groupId>
        <artifactId>moditect-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
      </plugin>
      <plugin>
        <artifactId>maven-source-plugin</artifactId>
      </plugin>
      <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
